Write a java program to get 2 numbers from the user and swap their values without any loss of data. You can make use of additional 3rdvariable for swapping.

Print the corresponding swapped values of the two numbers as output in the console.

Input format:
First input: an integer

Second input: an integer

Output format:

The output will be integers(swapped values)

Sample Input:

20

10

Sample Output:

10

20

import java.util.Scanner;  // importing the scanner function
public class Main
{
    public static void main(String args[])
    {
        Scanner sc=new Scanner(System.in); // creating a new object for the scanner function 
        int a,b,temp;  // specifing the variable names and datatypes
        a=sc.nextInt(); // to give input values 
        b=sc.nextInt(); // to give input values 
        temp=a; // 3rd variable using to swap the values in the variables 
        a=b;
        b=temp;
        System.out.println(a); // print the given input
        System.out.println(b); // print the given input
    }
}
